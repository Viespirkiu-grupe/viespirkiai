---
const { asmuo } = Astro.props;
if (!(asmuo.darboSkelbimai?.rows > 0)) return;
const { rows, limit, skelbimai } = asmuo.darboSkelbimai;
---

<div class="page-section mb-10" id="darboSkelbimai">
  <h2><a href="#darboSkelbimai" class="no-style">Darbo skelbimai</a></h2>
  {rows > limit && (
    <p class="text-muted text-sm mb-4">
      Rodomi tik {limit} naujausi skelbimai.
      <a href="?darboSkelbimaiLimit=max#darboSkelbimai">Rodyti visus ({rows})</a>
    </p>
  )}
  <div class="result-list">
    {skelbimai.map((s: any) => (
      <article class="result-card">
        <div class="result-card-body">
          <dl class="result-card-meta">
            <div><dt class="sr-only">Paskelbta</dt><dd>Paskelbta {s.ikelimo_data}</dd></div>
            <div><dt class="sr-only">Galioja</dt><dd>{s.galioja_nuo || 'nežinoma'} – {s.galioja_iki || 'nežinoma'}</dd></div>
          </dl>
          <h3 class="result-card-title">{s.profesijos_pareigybes_pav || 'Profesijos pareigybės pavadinimas nenurodytas'}</h3>
          <p class="text-sm text-muted mt-2 line-clamp-4">{s.darbo_aprasymas_lt || 'Darbo aprašymas nenurodytas'}</p>
        </div>
      </article>
    ))}
  </div>
</div>
