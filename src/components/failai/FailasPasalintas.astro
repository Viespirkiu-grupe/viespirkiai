---
import Layout from '../../layouts/Layout.astro';
import { getColorScheme, getFont } from '../../lib/failai.js';
const colorScheme = getColorScheme(Astro.cookies);
const font = getFont(Astro.cookies);
---
<Layout pageTitle="Failas pašalintas" pageDescription="" pageImage="https://viespirkiai.org/failai.png" colorScheme={colorScheme} font={font}>
  <main>
    <div class="empty-state">
      <h1 class="mb-2">Failas pašalintas</h1>
      <p class="text-muted text-sm">451 Unavailable For Legal Reasons — kol kas neturime daugiau informacijos.</p>
    </div>
  </main>
</Layout>
