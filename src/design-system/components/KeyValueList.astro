---
const { as = 'dl', className = '' }: { as?: 'dl' | 'div'; className?: string } = Astro.props;
const Tag = as;
const classes = ['kv-list', className].filter(Boolean).join(' ');
---

<Tag class={classes}>
  <slot />
</Tag>
