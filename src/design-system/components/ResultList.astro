---
interface Props {
  className?: string;
  bordered?: boolean;
}

const { className = '', bordered = true } = Astro.props;
const classes = [bordered ? 'result-list' : '', className].filter(Boolean).join(' ');
---

<div class={classes}>
  <slot />
</div>
