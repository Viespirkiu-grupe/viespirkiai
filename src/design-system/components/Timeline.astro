---
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
const classes = ['ds-timeline', className].filter(Boolean).join(' ');
---

<ul class={classes}>
  <slot />
</ul>
