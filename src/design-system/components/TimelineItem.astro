---
interface Props {
  active?: boolean;
  lineActive?: boolean;
  className?: string;
}

const {
  active = false,
  lineActive = false,
  className = '',
} = Astro.props;

const classes = ['ds-timeline-item', lineActive ? 'ds-timeline-item--line-active' : '', className].filter(Boolean).join(' ');
---

<li class={classes}>
  <span class:list={['ds-timeline-dot', active && 'ds-timeline-dot--active']}></span>
  <div class="ds-timeline-body">
    <slot />
  </div>
</li>
