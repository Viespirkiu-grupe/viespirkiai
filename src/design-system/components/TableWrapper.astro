---
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
const classes = ['table-wrapper', className].filter(Boolean).join(' ');
---

<div class={classes}>
  <slot />
</div>
