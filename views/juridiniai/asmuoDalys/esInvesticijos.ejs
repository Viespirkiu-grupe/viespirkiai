<% if (asmuo.esInvesticijos?.count > 0) { %>
<div class="page-section mb-6" id="esInvesticijos">
  <h2><a href="#esInvesticijos" class="no-style">ES investicijos (2014 programa)</a></h2>

  <% if (asmuo.esInvesticijos.count > asmuo.esInvesticijos.rows.length) { %>
    <p class="text-muted text-sm mb-4">
      Rodomos tik <%= Number(asmuo.esInvesticijos.limit).linksniuoti(["paraiška", "paraiškos", "paraiškų"]) %>.
      <a href="?esInvesticijosLimit=max#esInvesticijos">Rodyti visus (<%= asmuo.esInvesticijos.count %>)</a>
    </p>
  <% } %>

  <div class="result-list">
    <% asmuo.esInvesticijos.rows.forEach(item => { %>
      <%
        const suma = typeof item.galutineSuma === 'number' && Number.isFinite(item.galutineSuma)
          ? new Intl.NumberFormat('lt-LT', { style: 'currency', currency: 'EUR' }).format(item.galutineSuma)
          : null;
        const pabaiga = item.pabaigosData instanceof Date
          ? item.pabaigosData.toISOString().slice(0, 10)
          : item.pabaigosData
            ? new Date(item.pabaigosData).toISOString().slice(0, 10)
            : 'nenurodyta';
      %>
      <article class="result-card card-clickable">
        <div class="result-card-body">
          <dl class="result-card-meta">
            <div><dt class="sr-only">Pareiškėjas</dt><dd><%= item.pareiskejas %></dd></div>
            <div><dt class="sr-only">Kodas</dt><dd><span class="cell-mono"><%= item.kodas %></span></dd></div>
            <div><dt class="sr-only">Pabaiga</dt><dd>Iki <%= pabaiga %></dd></div>
          </dl>

          <h3 class="result-card-title">
            <a href="https://2014.esinvesticijos.lt/lt/finansavimas/paraiskos_ir_projektai/<%= item.slug %>">
              <% if (suma) { %><span class="cell-mono"><%= suma %></span> – <% } %><%= item.pavadinimas %>
            </a>
          </h3>

          <div class="result-card-footer">
            <span><%= item.busena %></span>
          </div>
        </div>
      </article>
    <% }) %>
  </div>
</div>
<% } %>
