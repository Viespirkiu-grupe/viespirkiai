<% if(asmuo.regitra && asmuo.regitra.rows > 0){ %>
    <div id="transportoPriemones">
        <h2><a href="#transportoPriemones" class="hashHref"><u>Transporto priemonės</u></a></h2>
        <% if(asmuo.regitra.rows > asmuo.regitra.limit) { %>
            <i>
                Rodomos tik <%= Number(asmuo.regitra.limit).linksniuoti(["transporto priemonė", "transporto priemonės", "transporto priemonių"]) %>.
                <a href="?transportoPriemonesLimit=max#transportoPriemones">
                    Rodyti visas (<%= asmuo.regitra.rows %>)
                </a>
            </i>
            <br><br>
        <% } %>
        <div class="tableContainer">
            <table style="width: auto; min-width: 70vw;">
                <thead>
                    <tr>
                        <th>Marke</th>
                        <th>Pavadinimas</th>
                        <th>Kategorija</th>
                        <th>Degalai</th>
                        <th>Galia (kW)</th>
                        <th>Vietos</th>
                        <th>Pirmoji registracija</th>
                        <th>Savininkas</th>
                    </tr>
                </thead>
                <tbody>
                    <% asmuo.regitra.transportoPriemones.forEach(function(auto){ %>
                        <%
                        var savininkas = "Ne";
                        if(auto.valdymoTeise == "A"){
                            savininkas = "Taip";
                        }
                        %>
                        <tr>
                            <td><%= auto.marke %></td>
                            <% if(auto.komercinisPavadinimas == "Nuasmeninta") { %>
                                <td><%= auto.komercinisPavadinimas %></td>
                            <% } else { %>
                                <td>
                                    <a href="https://duckduckgo.com/?t=h_&q=<%- encodeURIComponent(auto.marke) + " " + encodeURIComponent(auto.komercinisPavadinimas) %>&ia=images&iax=images" target="_blank" rel="noopener noreferrer">
                                        <%= auto.komercinisPavadinimas %>
                                    </a>
                                </td>
                            <% } %>
                            <td class="center"><%= auto.kategorijaKlase %></td>
                            <td class="center"><%= auto.degalai || "-" %></td>
                            <td class="center"><%= auto.galia || "-" %></td>
                            <td class="center"><%= (Number(auto.sedimuVietuSkaicius) + Number(auto.stovimuVietuSkaicius)) || "-" %></td>
                            <td class="center"><%= auto.pirmosiosRegistracijosData || "-" %></td>
                            <td class="center"><%= savininkas || "?" %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
<% } %>
