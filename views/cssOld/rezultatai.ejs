<style>
section.preResultOptions{
	display: flex;
	justify-content: space-between;
	align-items: center;
	gap: 8px;
	margin-block: 0px;
	padding-left: 0px;
	max-width: 1200px;
    margin-inline: auto;
}

@media (max-width: 768px) {
	section.preResultOptions{
		flex-direction: column;
		align-items: flex-start;
		gap: 8px;
	}
}


.results {
    display: flex;
    flex-direction: column;
    gap: 20px;
    margin-inline: 8px;
    margin-top: 24px;
}

.results h1{
    margin-bottom: 0px;
    text-decoration: underline;
}

.results-header{
    margin-bottom: 8px;
    display: flex;
    justify-content: space-between;
}

@media (max-width: 768px){
    .results-header{
        flex-direction: column;
        align-items: flex-start;
    }
}

.results-header .export-options a{
    display: inline-block;
    margin-left: 4px;
}

.no-results{
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 16px;
    height: 20vh;
}

@media (min-width: 1000px){
    .results {
        margin-inline: 7%;
    }
    .main-center{
        margin-inline: 7%;
    }
}

@media (max-width: 600px){
    .results .resultsNauji {
        border: none;
    }

    .results .resultsNauji .rezultatas{
        padding-inline: 0px;
    }

    .results .resultsNauji .rezultatas:focus,
    .results .resultsNauji .rezultatas:hover,
    .results .resultsNauji .rezultatas:active {
        box-shadow: none;
    }

}

.rezultatas {
    padding-top: 0px;
    padding-bottom: 24px;
}

.rezultatas-top {
    font-size: 14px;
    color: var(--gray);
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    margin-bottom: 4px;
}

.rezultatas-top a{
    color: var(--gray);
}

.rezultatas-top span{
    display: inline-block;
    padding-right: 8px;
    padding-block: 2px;
}

.rezultatas-title {
    font-size: 24px;
    font-weight: bold;
    display: flex;
    align-items: baseline;
    gap: 6px;
    margin-bottom: 6px;
    margin-top: 0px;
}

.rezultatas-title a {
    color: var(--black);
    text-decoration: none;
}

.rezultatas-title a:hover {
    text-decoration: underline;
}

.rezultatas-title span:first-child {
    font-weight: bold;
}

.rezultatas-bottom {
    display: flex;
    flex-direction: column;
    gap: 12px;
    padding-top: 4px;
    color: var(--dark-gray);

}

.rezultatas-bottom a {
    color: var(--gray);
    text-decoration: none;
}

.rezultatas-bottom a:hover {
    text-decoration: underline;
}

.rezultatai-nezinomas-total{
    display: inline-flex;
    justify-content: center;
    min-width: 16px;
}

.rezultatai-nezinomas-total.spin {
    animation: spin 0.5s linear infinite;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

.excerpt {
    color: var(--gray);
}

.resultsNauji{
    border: 1px solid var(--light-gray);
    max-width: 1200px;
    width: 100%;
    margin-inline: auto;
    border-radius: 8px;
}

.main-center{
    max-width: 1200px;
}

.resultsNauji .rezultatas:first-child {
    border-top-left-radius: 8px;
    border-top-right-radius: 8px;
}

.resultsNauji .rezultatas:last-child {
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}


.resultsNauji .rezultatas {
    padding: 28px;
    display: flex;
    align-items: flex-start;
    transition: box-shadow 0.2s ease-in-out;
}

.resultsNauji .rezultatas-left {
    flex: 1 1 auto;   /* fills remaining space */
    min-width: 0;     /* prevents overflow */
}


.resultsNauji .rezultatas-right{
    float: right;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    border-left: 1px solid var(--light-gray);
    padding-left: 12px;
    padding-right: 8px;
    padding-block: 8px;
    margin-top: 12px;
    margin-left: 12px;
    gap: 8px;
}

.resultsNauji .rezultatas-right .suma{
    font-size: 20px;
    font-weight: bold;
    font-family: monospace;
}

.resultsNauji .rezultatas-right .sumosTipas{
    color: var(--gray);
    font-size: smaller;
}

.resultsNauji .rezultatas-top{
    margin-top: 0px;
}

.vertical-line-spacer{
    width: 1px;
    background-color: var(--light-gray);
}

.resultsNauji .subtext{
	color: var(--gray);
	font-size: 14px;
}

.resultsNauji .rezultatas:hover {
    box-shadow: inset 0 0 0 1px var(--black);
}

.rezultatas {
    position: relative; /* needed for absolute overlay */
}

/* full-area overlay link behind content */
.fullIndentLink {
    position: absolute;
    inset: 0;             /* fill the article */
    z-index: 0;           /* behind content */
    pointer-events: auto; /* allow clicks */
    text-indent: -9999px; /* hide text if needed */
}

/* inner links stay above overlay and clickable */
.rezultatas a:not(.fullIndentLink),
.rezultatas span,
.rezultatas u,
.rezultatas abbr{
    position: relative;
    z-index: 1;
}

* {
    -webkit-tap-highlight-color: transparent;
}
</style>
