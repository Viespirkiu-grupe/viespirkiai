<!-- INTRO / INDEX -->
<section class="section" id="intro">
  <h1>Dizaino sistema</h1>
  <p class="text-muted text-sm mb-5">
    Šis puslapis yra komponentų katalogas — nuo <code>:root</code> tokenų iki UI komponentų.
    Žemiau pateikti pavyzdžiai ir jų HTML ištraukos (per <code>highlightCode()</code>).
  </p>

  <div class="preview preview-col">
    <div class="alert alert-info">
      <span class="alert-icon">ℹ</span>
      <div class="alert-body">
        <div class="alert-title">Pastaba</div>
        Komponentai remiasi tokenais (CSS custom properties). Tamsus režimas aktyvuojamas automatiškai per override'us.
      </div>
    </div>

    <div class="bg-light p-3 rounded">
      <div class="text-muted text-xs">Greitos nuorodos</div>
      <div class="d-flex flex-wrap gap-2 mt-2">
        <a class="btn btn-secondary btn-sm" href="#tokens">Tokens</a>
        <a class="btn btn-secondary btn-sm" href="#typography">Typography</a>
        <a class="btn btn-secondary btn-sm" href="#layout">Layout</a>
        <a class="btn btn-secondary btn-sm" href="#buttons">Buttons</a>
        <a class="btn btn-secondary btn-sm" href="#forms">Forms</a>
        <a class="btn btn-secondary btn-sm" href="#tables">Tables</a>
        <a class="btn btn-secondary btn-sm" href="#cards">Cards</a>
        <a class="btn btn-secondary btn-sm" href="#navigation">Navigation</a>
        <a class="btn btn-secondary btn-sm" href="#utilities">Utilities</a>
      </div>
    </div>
  </div>
</section>
