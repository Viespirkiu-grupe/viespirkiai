<style>
.map-container {
    border-radius: var(--radius-lg);
    overflow: hidden;
    border: var(--border);
}

.map-container .leaflet-container {
    font-family: var(--font-sans);
}

/* Sizes */
.map-sm  { height: 300px; }
.map-md  { height: 500px; }
.map-lg  { height: 70vh; }
.map-xl  { height: 90vh; }

/* Tile filter utilities */
.map-tiles-blur     { filter: blur(5px); }
.map-tiles-grayscale { filter: grayscale(1); }
.map-tiles-dim      { opacity: 0.5; }

.leaflet-control-zoom {
    border: var(--border) !important;
    border-radius: var(--radius-md) !important;
    overflow: hidden;
    box-shadow: none !important;
}

.leaflet-control-zoom-in,
.leaflet-control-zoom-out {
    width: 32px !important;
    height: 32px !important;
    line-height: 32px !important;
    font-size: var(--text-base) !important;
    font-family: var(--font-sans) !important;
    color: var(--black) !important;
    background: var(--white) !important;
    border: none !important;
    border-bottom: var(--border) !important;
    transition: background-color var(--transition) !important;
}

.leaflet-control-zoom-out {
    border-bottom: none !important;
}

.leaflet-control-zoom-in:hover,
.leaflet-control-zoom-out:hover {
    background: var(--lighter-gray) !important;
    color: var(--black) !important;
}

.leaflet-control-attribution {
    font-family: var(--font-sans) !important;
    font-size: var(--text-xs) !important;
    color: var(--gray) !important;
    background: var(--white) !important;
    box-shadow: none !important;
    border: none !important;
    border-top: var(--border) !important;
    border-left: var(--border) !important;
    border-radius: var(--radius-md) 0 0 0 !important;
    padding: 2px var(--space-2) !important;
    margin: 0 !important;
}

.leaflet-control-attribution a {
    color: var(--gray) !important;
}

.leaflet-control-attribution a:hover {
    color: var(--black) !important;
}
</style>
