<style>
header,
main {
	padding-inline: 16px;
}

header {
	margin-bottom: 24px;
}

section h1 {
	font-size: 2em;
}

h1.viespirkiai {
	font-size: 3em;
	margin-bottom: 4px;
	margin-top: 4px;
}
a {
	color: var(--link);
	padding-block: 4px;
}

a.noStyle {
	text-decoration: none;
	color: inherit;
}
sub,
sup {
	font-size: 12px;
}

sup {
	font-style: italic;
	font-weight: normal;
	color: var(--dark-gray);
}

pre {
	font-size: larger;
}

ul.file-list {
	list-style-type: none;
	padding: 0;
}

ul.file-list li {
	margin-bottom: 8px;
}

ul.file-list li a {
	color: var(--dark-gray);
}

ul.file-list li a:hover {
	text-decoration: underline;
}

kbr{
    display: inline-block;
    padding: 2px 6px;
    font-size: 0.8em;
    line-height: 1.2;
    color: var(--text);
    background-color: var(--lighter-gray);
    border: 1px solid var(--light-gray);
    border-radius: 4px;
}

.bold {
    font-weight: bold;
}

.underline {
    text-decoration: underline;
}

.no-wrap {
    text-wrap: nowrap;
}

mark {
    background-color: rgba(0,0,0,0); /* or your preferred color */
    color: var(--black);
    font-weight: bold; /* optional: make it bold */
    text-decoration: underline; /* optional: underline it */
    display: inline;          /* ensure it stays inline */
    padding: 0;               /* optional: remove extra padding */
    margin: 0;                /* optional */
}

summary{
    cursor: pointer;
}

p{
    line-height: 1.5;
}

h2{
    margin-bottom: 14px;
}

h3{
    margin-block: 16px;
}

h4{
    margin-block: 12px;
}

dd{
    margin-left: 0px;
}

.visually-hidden {
  position: absolute !important;
  width: 1px !important;
  height: 1px !important;
  padding: 0 !important;
  margin: -1px !important;
  overflow: hidden !important;
  clip: rect(0, 0, 0, 0) !important; /* fallback */
  clip-path: inset(50%) !important;   /* modern replacement */
  white-space: nowrap !important;
  border: 0 !important;
}

.subtext{
    color: var(--gray);
}

blockquote {
    border-left: 4px solid var(--light-gray);
    margin-left: 0;
    margin-right: 0;
    padding-left: 16px;
    color: var(--dark-gray);
    font-style: italic;
}

</style>
