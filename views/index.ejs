<!DOCTYPE html>
<html lang="lt">
	<head>
	    <%- include('head.ejs', {
            pageTitle: "Viešpirkiai",
            pageDescription: `  `,
            pageImage: `https://viespirkiai.top/index.png`,
            customHead
        }) %>
	</head>
	<body>
		<%- include('header.ejs') %>
		<main>
			<%- include('search.ejs', {values, usedHiddenFields, numberOfResults}) %>
			<% if(naujaPaieska) { %>
			    <%- include('resultsModern.ejs', {values, usedHiddenFields, data, currentPage, pageCount, queryParams, galimaEksportuoti}) %>
			<% } else { %>
			    <%- include('results.ejs', {data, currentPage, pageCount, queryParams, galimaEksportuoti}) %>
			<% } %>
		</main>
		<%- include('footer.ejs') %>
	</body>
</html>
